staging-settings:
  user-workflow:
    workflow-name: "private-signal-swarm-aggregate-staging"
    deployment-registry: "private"
  workflow-artifacts:
    workflow-path: "./src/workflow.ts"
    config-path: "./config.staging.json"
    secrets-path: "../../secrets.yaml"
