# CRE environment (single root .env — legacy cre/.env still loaded as fallback if present)
.env
*.env
.env.simulate

# Build artifacts
*.cre_build_tmp.js
binary.wasm.br.b64
node_modules/

# CRE state
state/
