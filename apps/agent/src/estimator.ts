export class Estimator {
  compute(data: unknown): number {
    // TODO: Replace with actual estimation logic
    // For now, return a random value to simulate different agents
    return 100 + Math.random() * 10;
  }
}