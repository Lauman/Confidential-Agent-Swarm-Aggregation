{
  "name": "@private-signal-swarm/agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "echo \"agent: no auto-run in dev (would submit stray ballots to the demo round) — run manually: pnpm --filter @private-signal-swarm/agent start <proposalRef>\" && exit 0",
    "start": "node dist/main.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@private-signal-swarm/confidential-core": "workspace:*",
    "@private-signal-swarm/types": "workspace:*",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  }
}